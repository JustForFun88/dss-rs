# tests/corpus/gaps — the STAGING family for not-yet-ported coverage

Hand-written / generated DSS decks for everything the Rust engine does not
port yet: the Phase-4–7 feature deferrals whose only blocker was a missing
corpus deck (the WP7.9 "zero corpus cases → skip" anti-pattern, called out in
`PHASE8_PLAN.md` §1) **and** the unported element classes (Isource, AutoTrans,
GICLine, GICTransformer, GICsource). The execution plan that consumes them is
**`GAPS_PLAN.md`** at the repo root — read it first; it records per-deck
determinism proofs and gate strategy.

**"Gaps" names a deck's state, not its nature** (GAPS_PLAN §3.1): a deck lives
here only while its feature is unported (`pending: true` in `manifest.json` —
the gate asserts the Rust engine errors *loudly*). The WP that ports the
feature flips `pending: false`, proves the live compare green, and **moves the
deck to its permanent family** — `../asymmetric/` (static element coverage),
`../controls/` (control/time-series coverage), or `../modes/` (solve modes /
algorithms / input formats; created at the first such graduation). The
GAPS_PLAN exit sweep (WPG.17) deletes this then-empty directory.

Element coverage follows the deck matrix {snapshot × time-series × combined
snapshot→time-series} × {micro × midi} where the mode exists upstream; the
`midi_*` decks are generated by `tools/decks/gen_midi_decks.py` on the shared
IEEE123-class scaffold.

Every deck was validated against the pinned oracle (dss-python 0.15.7, see
`tools/golden/PIN.txt`): it compiles, solves, converges, is **bit-identical
across two separate oracle processes** (this matters — FPC `mathutil.pas`
time-seeds the RNG per process, so only RNG-free / `random=none` paths can be
pinned), and is **feature-sensitive** (removing the feature under test provably
changes the oracle output).

| Deck | Pins | WP |
|---|---|---|
| `shape_binfiles.dss` | LoadShape `SngFile/DblFile/PQCSVFile`, TShape/PriceShape binary, GrowthShape `CSVFile` | WPG.1 |
| `generaltime.dss` | `Set mode=Time` (`SolveGeneralTime`) | WPG.2 |
| `ld1.dss`, `ld2.dss` | `Set mode=LD1/LD2` + `Set LDCurve=` (load-duration) | WPG.3 |
| `monte1.dss`, `monte2.dss`, `monte3.dss` | `Set mode=M1/M2/M3` under `Set random=none` | WPG.4 |
| `montefault.dss` | `Set mode=MF` (single Fault ⇒ deterministic pick) | WPG.4 |
| `autoadd.dss` | `Set mode=AutoAdd` capacity search (winner: bus `b3`) | WPG.5 |
| `newton.dss` | `Set algorithm=Newton` | WPG.6 |
| `capcontrol_follow.dss` | CapControl `type=Follow` + `ControlSignal=` | WPG.7 |
| `reactor_rlcurve.dss` | Reactor `RCurve`/`LCurve` under harmonics | WPG.8 |
| `invcontrol_expmodel.dss` | InvControl `ControlModel=1` (Exponential/TPICtrl) | WPG.9 |
| `invcontrol_storage_vw.dss` | InvControl `mode=voltwatt` over Storage | WPG.10 |
| `invcontrol_storage_vv_vw.dss` | InvControl `combimode=VV_VW` over Storage | WPG.10 |
| `storagecontroller_seasonal.dss` | StorageController seasonal targets + `Set SeasonRating/SeasonSignal` | WPG.11 |
| `isource_snap.dss` | Isource snapshot: 1-ph trio, explicit `Bus2`, `sequence=neg` | WPG.14 |
| `isource_daily.dss` | Isource `daily=` shape drive (8 h, meter + monitors) | WPG.14 |
| `isource_both.dss` | Isource snapshot→daily transition (cross-mode state leaks) | WPG.14 |
| `isource_harm.dss` | Isource spectrum injection under `mode=harmonics` (+ `scantype=zero`) | WPG.14 |
| `midi_isource_asym.dss`, `midi_isource.dss`, `midi_isource_both.dss` | the same three modes at midi scale (94 nodes) | WPG.14 |
| `autotrans_snap.dss` | AutoTrans 3-wdg s/w/d (AutoAuto 330 MVA) + 2-wdg unit; `WdgCurrents` probe | WPG.15 |
| `autotrans_reg.dss` | RegControl on AutoTrans common winding (daily, event log) | WPG.15 |
| `autotrans_both.dss` | AutoTrans snapshot→RegControl-daily transition | WPG.15 |
| `autotrans_gic.dss` | AutoTrans `GICBuildYTerminal` (< 0.51 Hz Rdc-only branch) | WPG.15 |
| `midi_autotrans_asym.dss`, `midi_autotrans.dss`, `midi_autotrans_both.dss` | the same modes at midi scale (100 nodes, 10 tap events) | WPG.15 |
| `gicline_gic.dss` | GICLine: `Volts` + `EN/EE` geodesy + blocking-`C` units @ 0.1 Hz | WPG.16 |
| `gictransformer_gic.dss` | GICTransformer GSU/YY/Auto, `R1/R2` + `%R1` specs, `VarCurve` | WPG.16 |
| `gicsource_gic.dss` | GICsource named-Line splice (`GIC_<name>` bus, Bus2 rewrite) | WPG.16 |
| `gic_midi.dss` | all three GIC classes on a 6-substation 345 kV ring (33 nodes) | WPG.16 |

(No async/both decks for the GIC classes: none has a shape reference or any
time-varying drive upstream — their corpus usage is a 0.1 Hz quasi-DC
snapshot.)

`ls8.sng`, `ls8.dbl`, `lspq8.csv`, `t8.sng`, `p8.dbl`, `g4.csv` are the committed
input fixtures for `shape_binfiles.dss` (rebuildable via
`tools/corpus/gen_gaps_binshapes.py`).

Unlike `../electricdss-tst/` this directory is **not** vendored (no SHA256SUMS
entry, not managed by `tools/corpus/vendor.py`) — it is first-party. Deck edits
require re-running the GAPS_PLAN §3 validation protocol (two-process
determinism + feature sensitivity) on the pinned oracle.
