# Oddie-bridge pin — the OFFICIAL-EPRI-binary oracle env. This is a SEPARATE
# venv from tools/golden/PIN.txt (dss-python 0.15.7 and 0.16.0b2 cannot coexist
# in one environment/process). dss-python here is only the bridge (IOddieDSS);
# the engine under test is the vendored EPRI OpenDSSDirect.dll per revision
# (tools/opendss/bin/, pinned in revisions.json expect_version).
python==3.12.x
# The two betas are vendored as wheels in tools/opendss/wheels/ (SHA256SUMS
# there); install with --find-links tools/opendss/wheels — no PyPI-pre-release
# dependency.
dss-python==0.16.0b2
dss-python-backend==0.15.0b4
# For dsspy_validation/compare_outputs.py (CSV + CIM-XML diffing only; the
# engine comparison itself needs none of these). NOTE: the `capi` engine that
# venv/harness exposes is dss_capi 0.15.0b4 — NOT the repo's pinned 0.14.5
# oracle. Upstream inventory only; never feeds goldens or the gate.
pandas==3.0.3
xmldiff==3.0
