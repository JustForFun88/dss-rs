# WASM user-model toolchain pin — WP-WM.0 P4 (initial; WM.2 appends the
# committed-fixture hashes). Fixtures/goldens regenerate MANUALLY ONLY with
# exactly these versions (plan §2.6/§2.9-4; evidence docs/wasm/probes/p4_toolchain.txt).
#
# host interpreter (product dependency, [workspace.dependencies] at WM.1)
wasmi==1.0.9            # default-features = false, features = ["simd"]
                        # pure-Rust closure: wasmi_core/wasmi_collections/wasmi_ir 1.1.0,
                        # wasmparser 0.228.0, bitflags 2.13.1, libm 0.2.16, spin 0.9.9
# guest fixture toolchain (tools/wasm_usermodel/models/*, WM.2)
rustc==1.96.0           # stable (ac68faa20 2026-05-25), x86_64-pc-windows-msvc host
target=wasm32-unknown-unknown
profile=release         # locked in the fixture crate Cargo.toml: opt-level=3,
                        # lto=true, codegen-units=1, panic=abort, strip=debuginfo
# native twin toolchain (plan A — vendored IndMach012a.dpr, oracle-side golden gen)
fpc==3.2.2              # ppcrossx64 x86_64-win64 cross, -Mdelphi -O2
# twin source: ABI RE-FREEZE TO r4133 (user decision 2026-07-19) — build_native.ps1
#   now compiles .inputs/electricdss-code-r4133-trunk/Version8/Source/IndMach012a/
#   IndMach012a.dpr (was r3723). The IndMach012a example dir is byte-identical
#   r3723->r4133 (docs/wasm/probes/p8_indmach012a_math_diff.txt); the only delta is
#   GeneratorVars.pas (deltaQNom => 252-B native TGeneratorVars, p8_offsets_r4133.txt).
#   The r4133 twin LOADS + RUNS in the r4133 engine via the dss-epri/epri-worker
#   bridge (p8_twin_r4133_bridge.txt). twin_probe.py drives it with the 252-B ctypes
#   image; every recorded value is BIT-IDENTICAL to the r3723 image (model math
#   unchanged, deltaQNom engine-only), so twin_expected.rs and the committed .wasm
#   are UNCHANGED (fixture self-gate stays bit-exact green).
# EPRI r4133 bridge DLL (twin-load oracle): tools/opendss/bin/r4133/OpenDSSDirect.dll
#   Version 11.0.0.1 (64-bit) - Charlottesville (tools/opendss/revisions.json)
# oracle (unchanged, tools/golden/PIN.txt governs): dss-python 0.15.7 / backend 0.14.5
#
# ---- committed fixture artifacts (WM.2) — regenerate MANUALLY ONLY ----
# build command (exact, reproducible from a clean target dir; sets
# RUSTFLAGS=--remap-path-prefix=<crate>=/indmach012a and copies the artifact):
#   pwsh tools/wasm_usermodel/build_wasm.ps1
# built with: cargo +stable 1.96.0 (30a34c682 2026-05-25), wasm32-unknown-unknown
# tests/fixtures/wasm/indmach012a.wasm  (79045 bytes)
sha256(indmach012a.wasm)=1849db0cbc52352afa50cf98060995b62c3d7cdd9cb3438b5961c2d38e9b0ebd
# numeric spec: native twin (build_native.ps1, FPC 3.2.2) probed by
# twin_probe.py -> docs/wasm/probes/p6_twin_expected.txt; the port is pinned
# BIT-EXACT against it by models/indmach012a/tests/twin_parity.rs (twin_expected.rs
# is generated by `twin_probe.py <dll> --rust`).

# ==== WM.4 fixture: wm4model (Storage DynaDLL + PVSystem UserModel) ====
# A small 3-phase inverter model: constant admittance in power flow, first-order
# current lag in dynamics, dispatched on TDynamicsRec.SolutionMode. ONE module
# serves BOTH the PVSystem `UserModel=` (15-fn) and Storage `DynaDLL=` (13-fn)
# gates. Reads inputs from V + TDynamicsRec only (no host callbacks, no
# TStorageVars/TPVSystemVars image crosses) — frozen ABI unchanged.
# crate: tools/wasm_usermodel/models/wm4model/ (workspace-excluded, cdylib+rlib).
#
# ---- committed wasm fixture (WM.4) — regenerate MANUALLY ONLY ----
# build command (exact, reproducible from a clean target dir):
#   pwsh tools/wasm_usermodel/build_wm4model_wasm.ps1
# built with: cargo +stable 1.96.0 (30a34c682 2026-05-25), wasm32-unknown-unknown
# tests/fixtures/wasm/wm4model.wasm  (60587 bytes)
sha256(wm4model.wasm)=dad9e74bac54316b396982e7cda14ae9791d8b5d3365833f45bbb9c76b8b77b7
# native twin (r4133-oracle side, plan §2.6 plan B — the SAME Rust model core as a
# native cdylib; NOT committed, built on demand for golden generation):
#   pwsh tools/wasm_usermodel/build_wm4model_native.ps1  -> %TEMP%/wm4model_native_twin/wm4model.dll
# oracle: EPRI r4133 bridge (tools/opendss/bin/r4133/OpenDSSDirect.dll, epri-worker),
#   loads the twin as Storage DynaDLL / PVSystem UserModel; goldens generated by
#   crates/dss-epri/tests/gen_wasm_usermodels_wm4.rs (env-gated WASM_TWIN_DLL, manual).
# decks: tools/golden/wasm_decks/wasm_{pv_pflow,storage_dyn}.dss (@FIXTURE@ twin form)
# goldens: tests/golden/wasm_usermodels/wasm_{pv_pflow,storage_dyn}.json
# hermetic replay gate: crates/dss-core/tests/wasm_usermodels_wm4.rs
